1.begin() 함수로 그 다음 화면으로 넘어가게 만든다
	1) fadeOut, fadeIn으로 
	2) goNext(qidx)로 순서를 넘겨준다
2.goNext(qidx)
	0) if endPoint === 12면 goResult(); return		
	1) 질문칸에 data.js를 넘겨준다.
	2) 대답칸을 만들어준다(for구문으로 answerButton(내용, qidx, i(몇번째 선택했는지)함수 호출)

3.answerButton(내용, qIdx, idx)
	1) createElement('button') 만들어서 넣어주기
	2) button.innerHTML = 내용
	3) button.addEventListener("click", function () {
	let children = querySelectAll('.answerList');부르기
	for(let i = 0; i < children.length(3개일것); i++){
	children.disabled = true;
	children.style.animation = 'fadeOut 0.5s' 
	webkitanimation도
		setTimeout(()=>{
	for문 children.style.display ='none'
	goNext(++qIdx)

결과
4. goResult()
	fadeOut과 fadeIn 똑같이 만들어준다
	setResult()
	calResult()
5.calResult 는 select배열함수를 써서 가장 높은 숫자를 뽑아낸다.
6. setResult 는 point에 담고 화면에 뿌릴 준비

기타
7. status bar

8. 공유하기

9. 공유페이지 구현